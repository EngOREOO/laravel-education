<div dir="rtl">

# 🚀 شرح Laravel كامل من الصفر 

### المشروع + قاعدة البيانات + نظام تسجيل الدخول

الشرح ده هيوريك ازاي تعمل مشروع لارافيل جديد، تظبط الداتابيز، وتركب نظام التسجيل والدخول باستخدام لارافيل بريز.

---

## 📋 الحاجات اللي محتاجها الأول

تأكد إن الحاجات دي موجودة على جهازك:

- **PHP & Composer**
- **Node.js & NPM**  
- **XAMPP** - أو أي سيرفر محلي عشان تشغل MySQL

---

## 🛠️ الخطوات كاملة

### 1️⃣ إنشاء المشروع

افتح الترمنال أو الـ CMD في المجلد اللي عايز تحفظ فيه المشروع، واكتب الأمر ده:

```bash
composer create-project laravel/laravel my-app
cd my-app
```

**ملحوظة:** بدل `my-app` بأي اسم إنت عايزه للمشروع بتاعك

---

### 2️⃣ ظبط الداتابيز

1. تأكد إن MySQL شغال (من XAMPP مثلاً)
2. افتح المتصفح وروح على اللينك ده:
   ```
   http://localhost/phpmyadmin
   ```
3. اضغط على **New** من الشمال
4. اكتب اسم الداتابيز، مثلاً: `laravel_breeze_db`
5. اضغط على **Create**

---

### 3️⃣ ربط المشروع بالداتابيز

ارجع لـ VS Code وافتح ملف `.env` وغيّر الإعدادات دي:

```env
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=laravel_breeze_db
DB_USERNAME=root
DB_PASSWORD=
```

**شرح الإعدادات:**
- `DB_DATABASE` - حط اسم الداتابيز هنا
- `DB_USERNAME` - اليوزر الافتراضي (root)
- `DB_PASSWORD` - سيبه فاضي لو مفيش باسورد

---

### 4️⃣ تنصيب لارافيل بريز🍃

عشان تضيف نظام الدخول والتسجيل واستعادة الباسورد، اكتب الأوامر دي:

```bash
composer require laravel/breeze --dev
php artisan breeze:install
```

**انتبه:** الترمنال هيسألك كام سؤال:
- اختار الـ stack اللي تحبه (ننصح بـ Blade لو مبتدئ)
- عايز Dark Mode؟ (اختياري)
- عايز ملفات PHPUnit للتست؟ (اختياري)

---

### 5️⃣ عمل الجداول وبناء ملفات التصميم

دلوقتي هنحول الـ Migration لجداول فعلية في الداتابيز ونحمل ملفات CSS و JS:

```bash
php artisan migrate
npm install
npm run build
```

**ملحوظة:** ممكن تستخدم `npm run dev` لو عايز تشتغل بشكل مباشر

---

### 6️⃣ تشغيل المشروع 🏁

كده كل حاجة تمام! شغّل السيرفر بالأمر ده:

```bash
php artisan serve
```

بعدها روح على المتصفح وافتح اللينك ده:
```
http://127.0.0.1:8000
```

---

## ✅ النتيجة النهائية

هتلاقي لينكات **Log in** و **Register** فوق في اليمين وشغالة تمام! 🎉

---

## 📝 ملاحظات مهمة

**لو حصلت مشاكل:**
- تأكد من إعدادات الملف `.env` خصوصاً اسم الداتابيز
- تأكد إن MySQL شغال من XAMPP

**للتطوير والتعديل:**
- تقدر تعدل على صفحات التسجيل والدخول من المسار ده:
  ```
  resources/views/auth
  ```
- لو شغال development مستمر، استخدم `npm run dev` بدل `build`

---

**صُنع بحب ❤️ باستخدام لارافيل وبريز**

</div>
