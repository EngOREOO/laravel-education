<style>
body {
    direction: rtl;
    text-align: right;
}

pre, code {
    direction: ltr;
    text-align: left;
}

code {
    unicode-bidi: embed;
}

pre code {
    unicode-bidi: embed;
}
</style>

<div dir="rtl">

# شرح شامل لنموذج MVC في Laravel 🎯

## مقدمة

هنتكلم النهاردة عن **MVC** - اللي هو **Model-View-Controller** - ودي طريقة تنظيم الكود في تطبيقات الويب عشان يكون الكود منظم وسهل الصيانة.

---

## إيه يعني MVC؟ 🤔

**MVC** هو نمط تصميم (Design Pattern) بيفصل الكود لـ 3 أجزاء رئيسية:

1. **Model (الموديل)** - بيمثل البيانات والقواعد البيزنيس
2. **View (الفيو)** - بيمثل الـ UI اللي المستخدم بيشوفه
3. **Controller (الكونترولر)** - اللي بيربط بين الـ Model والـ View

---

## 1. Model (الموديل) 📦

### إيه هو الـ Model؟

الـ **Model** هو الكلاس اللي بيمثل البيانات في قاعدة البيانات. كل Model بيمثل جدول (table) في قاعدة البيانات.

### العلاقة بين Model و Database Table

**كل Model مربوط بجدول معين في قاعدة البيانات.**

#### ليه لازم اسم الـ Model يكون بالمفرد واسم الجدول بالجمع؟

Laravel بيعتمد اسم الـ Model المفرد عشان ينظم العلاقة تلقائيًا بين الكلاس والجدول. يعني مثلاً:

- لو عندك جدول اسمه `users` (جمع)، لازم يكون عندك Model اسمه `User` (مفرد).
- ده لأن Laravel بيعمل عملية تحويل تلقائي للأسماء (singular/plural) لما يربط الموديل بالجدول، فلو خالفت القاعدة دي ممكن Laravel ميعرفش يلاقي الجدول الصح أو يحصل لخبطه.

**ليه ما ينفعش يكون الموديل اسمه Users؟**

- لو سميت الموديل بتاعك `Users` (جمع)، Laravel هيدور أوتوماتيكي على جدول اسمه `userss` (ويزود s تاني)، وده طبعًا مش موجود.
- بالتالي الأفضل دائمًا نسمي الموديل بالمفرد (مثال: User, Post, Product) والجدول بالجمع (users, posts, products).

#### مثال من مشروعنا: User Model

خلينا نشوف الـ Model بتاعنا:

**شرح خاصية `$fillable`:**

الخاصية `$fillable` دي بتحدد الأعمدة (columns) اللي مسموح تعديلها أو إضافتها بشكل جماعي للجدول. يعني:

- لو كتبت `protected $fillable = ['name', 'email', 'password'];` يبقى بس الأعمدة دول هما اللي Laravel هيقبل يتعمل لهم إضافة أو تعديل جماعي.
- لو حاولت تبعت عمود زيادة مش مكتوب في `$fillable` (زي ما قلتلك - الكولمز في الفيلابل هنا يعني الجدول بيقول للموديل: "خلي بالك أنا هقبل إنك تضيف الحاجات دي بس، لو جبت كولم زيادة مش موجود في الفيلابل مش هقبلها منك وهخزنها null يعني هيبقى فاضي").
- العمود اللي مش مذكور في `$fillable` حتى لو موجود فعلاً في الجدول، Laravel مش هيسمحله يتغير بشكل جماعي، وده حماية للبيانات من التلاعب.

<div dir="ltr">

```php
// app/Models/User.php
namespace App\Models;

use Illuminate\Database\Eloquent\Factories\HasFactory;
use Illuminate\Foundation\Auth\User as Authenticatable;
use Illuminate\Notifications\Notifiable;

class User extends Authenticatable
{
    use HasFactory, Notifiable;

    protected $fillable = [
        'name',
        'email',
        'password',
    ];

    protected $hidden = [
        'password',
        'remember_token',
    ];

    protected function casts(): array
    {
        return [
            'email_verified_at' => 'datetime',
            'password' => 'hashed',
        ];
    }
}
```

</div>

**شرح الكود:**
- الـ `User` Model ده مربوط بجدول `users` في قاعدة البيانات
- الـ `$fillable` دي الأعمدة (columns) اللي ممكن نعدل فيها البيانات
- كل عمود في الجدول = خاصية (property) في الـ Model

#### مثال: جدول Users في قاعدة البيانات

خلينا نشوف الـ Migration اللي بيعمل الجدول:

<div dir="ltr">

```php
// database/migrations/0001_01_01_000000_create_users_table.php
Schema::create('users', function (Blueprint $table) {
    $table->id();                    // id
    $table->string('name');          // name
    $table->string('email')->unique(); // email
    $table->timestamp('email_verified_at')->nullable();
    $table->string('password');      // password
    $table->rememberToken();
    $table->timestamps();            // created_at, updated_at
});
```

</div>

**العلاقة:**
- الجدول اسمه `users` في قاعدة البيانات
- الـ Model اسمه `User` في الكود
- كل عمود في الجدول (`id`, `name`, `email`, `password`) = خاصية في الـ Model

### إزاي بنستخدم الـ Model؟

#### مثال 1: جلب المستخدم الحالي (من ProfileController)

<div dir="ltr">


#### مثال 2: إنشاء مستخدم جديد (من RegisteredUserController)

**الخطوة الأولى: الفاليديشن (Validation)**

<div dir="ltr">

```php
// app/Http/Controllers/Auth/RegisteredUserController.php
$request->validate([
    'name' => ['required', 'string', 'max:255'],
    'email' => ['required', 'string', 'lowercase', 'email', 'max:255', 'unique:'.User::class],
    'password' => ['required', 'confirmed', Rules\Password::defaults()],
]);
```

</div>

**شرح الفاليديشن بشكل عامي:**

الفاليديشن (Validation) ده زي ما يكون **حارس الأمن** قدام الباب. قبل ما ندخل أي بيانات لقاعدة البيانات، لازم نتأكد إنها صح ومطابقة للشروط.

**مثال عملي:** تخيل إنك في مكتب البريد عايز ترسل طرد. الحارس هيشوف:

- الطرد موجود؟ → `required`
- العنوان مكتوب صح؟ → `string, max:255`
- العنوان مش مكرر؟ → `unique`

**شرح كل قاعدة:**

**1. قاعدة الاسم (name):**

```
'name' => ['required', 'string', 'max:255']
```

- `required` → **مطلوب** → يعني لازم المستخدم يكتب اسمه، مش ممكن يخليه فاضي
- `string` → لازم يكون **نص** (مش رقم مثلاً)
- `max:255` → **أقصى طول 255 حرف** → يعني لو المستخدم كتب اسم طويل جداً، مش هنقبله

**2. قاعدة الإيميل (email):**

```
'email' => ['required', 'string', 'lowercase', 'email', 'max:255', 'unique:'.User::class]
```

- `required` → **مطلوب** → لازم يكتب إيميل
- `string` → لازم يكون **نص**
- `lowercase` → **يحوله لحروف صغيرة** → يعني لو كتب `Ahmed@Example.com` هيبقى `ahmed@example.com`
- `email` → لازم يكون **صيغة إيميل صحيحة** → يعني لازم يكون فيه @ و domain (مثل: .com)
- `max:255` → **أقصى طول 255 حرف**
- `unique:'.User::class` → **مش مكرر** → يعني لازم الإيميل يكون جديد، مش موجود قبل كده في جدول users. لو موجود، هيطلع error

**3. قاعدة الباسورد (password):**

```
'password' => ['required', 'confirmed', Rules\Password::defaults()]
```

- `required` → **مطلوب** → لازم يكتب باسورد
- `confirmed` → **مؤكد** → يعني لازم يكتب الباسورد مرتين (الباسورد + تأكيد الباسورد) واتنينهم يكونوا متطابقين
- `Rules\Password::defaults()` → **قواعد الباسورد الافتراضية** → يعني الباسورد لازم يكون قوي (مش ضعيف زي "123456")

**لو الفاليديشن نجح:**

لو كل البيانات صح ومطابقة للشروط، Laravel هيواصل التنفيذ. لو في حاجة غلط، Laravel هيوقف التنفيذ ويرجع error للمستخدم.

**الخطوة الثانية: إنشاء المستخدم**

<div dir="ltr">

```php
$user = User::create([
    'name' => $request->name,
    'email' => $request->email,
    'password' => Hash::make($request->password),
]);
```

</div>

**شرح العملية:**

**الخطوة الأولى: الفاليديشن عمل تشيك**

بعد ما الفاليديشن عمل **تشيك** واتأكد إن البيانات صح، هنواصل للخطوة التالية.

**الخطوة الثانية: بنروح للموديل**

بناخد البيانات اللي تم التحقق منها ونقول للـ Model (الموديل):

> **"يا User Model (موديل المستخدم)، بما إنك عندك صلاحية تضيف للجدول (عشان الأعمدة دي موجودة في `$fillable`)، ضيفلنا الراجل ده كـ user (مستخدم) جديد في قاعدة البيانات"**

**شرح بسيط:**

- الفاليديشن عمل تشيك على البيانات ✅
- البيانات صح ✅
- دلوقتي بنروح للموديل
- الموديل عنده صلاحية يضيف للجدول (لأن الأعمدة موجودة في `$fillable`)
- بنطلب منه يضيف المستخدم الجديد

**رسمة توضيحية للعملية:**

```
                    ┌─────────────────────────────────────┐
                    │  1. المستخدم يملأ الفورم            │
                    │     ويرسل البيانات                  │
                    │     (name, email, password)         │
                    └────────────┬────────────────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────────────────┐
                    │  2. Controller:                     │
                    │     $request->validate([...])       │
                    │     الفاليديشن يعمل تشيك            │
                    └────────────┬────────────────────────┘
                                 │
                                 ↓
                         ┌───────┴───────┐
                         │ البيانات صح؟  │
                         └───────┬───────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ↓                         ↓
    ┌────────────────────────┐   ┌────────────────────────┐
    │ نعم (صح)               │   │ لا (غلط)               │
    │ 3. Validation Error  نجح      │   │ ترجع              │
    │ البيانات صح            │   │ البيانات غلط           │
    └────────────┬───────────┘   └────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────┐
    │  4. User Model يفحص $fillable       │
    │                                      │
    │     protected $fillable = [         │
    │         'name',      (موجود)        │
    │         'email',     (موجود)        │
    │         'password',  (موجود)        │
    │     ];                               │
    └────────────┬────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────┐
    │  5. User::create([...])             │
    │     الموديل يضيف البيانات           │
    │     في قاعدة البيانات               │
    └────────────┬────────────────────────┘
                 │
                 ↓
    ┌─────────────────────────────────────┐
    │  6. نجاح!                           │
    │     المستخدم الجديد موجود          │
    │     في الجدول                       │
    └─────────────────────────────────────┘
```

**رسمة توضيحية: لو جينا نديله كولمز مش موجودة في `$fillable`:**

```
                    ┌─────────────────────────────────────┐
                    │  1. Controller يحاول يضيف بيانات    │
                    │     User::create([                  │
                    │         'name' => 'Ahmed',          │
                    │         'email' => 'ahmed@...',     │
                    │         'password' => '123456',     │
                    │         'admin' => true,    (X)     │
                    │         'balance' => 1000,  (X)     │
                    │     ]);                             │
                    └────────────┬────────────────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────────────────┐
                    │  2. User Model يفحص $fillable       │
                    │                                      │
                    │     protected $fillable = [         │
                    │         'name',      (موجود)        │
                    │         'email',     (موجود)        │
                    │         'password',  (موجود)        │
                    │         // 'admin' => (مش موجود)    │
                    │         // 'balance' => (مش موجود)  │
                    │     ];                               │
                    └────────────┬────────────────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────────────────┐
                    │  3. Laravel يتعامل مع البيانات:     │
                    │                                      │
                    │     name      <- يتم حفظه           │
                    │     email     <- يتم حفظه           │
                    │     password  <- يتم حفظه           │
                    │     admin     <- يتم تجاهله         │
                    │     balance   <- يتم تجاهله         │
                    └────────────┬────────────────────────┘
                                 │
                                 ↓
                    ┌─────────────────────────────────────┐
                    │  4. النتيجة في قاعدة البيانات:      │
                    │                                      │
                    │     ┌──────┬────────┬─────────┐    │
                    │     │ name │ email  │ password│    │
                    │     ├──────┼────────┼─────────┤    │
                    │     │Ahmed │ ahmed@ │ 123456  │    │
                    │     └──────┴────────┴─────────┘    │
                    │                                      │
                    │     تحذير: admin و balance          │
                    │        مستجلوش هيبقوا ظاهرين انهم null                    │
                    │        (مش موجودين في $fillable)    │
                    └─────────────────────────────────────┘
```

**ملخص:**

- لو الكولم موجود في `$fillable` → **هيتم حفظه** ✅
- لو الكولم مش موجود في `$fillable` → **مش هيتم حفظه** ❌ (Laravel هيتجاهله)

ده عشان **الأمان** - عشان مننفعش نضيف بيانات خطيرة للجدول بدون ما نحددها في `$fillable` الأول.

**شرح كل سطر:**

**السطر الأول:**

```php
$user = User::create([...])
```

- بنستخدم الـ Model عشان نضيف مستخدم جديد في قاعدة البيانات
- `create()` دي method جاهزة من Laravel
- بتعمل `INSERT` في قاعدة البيانات
- بتخزن النتيجة في المتغير `$user`

**السطر الثاني:**

```php
'name' => $request->name
```

- بناخد الاسم من الـ Request
- نحطه في العمود `name` في الجدول

**السطر الثالث:**

```php
'email' => $request->email
```

- بناخد الإيميل من الـ Request
- نحطه في العمود `email` في الجدول

**السطر الرابع:**

```php
'password' => Hash::make($request->password)
```

- بناخد الباسورد من الـ Request
- **بنعمله hash** (تشفير) عشان الأمان
- نحطه في العمود `password` في الجدول

**ليه بنعمل hash للباسورد؟**

عشان الأمان! لو حد شاف قاعدة البيانات، مش هيقدر يعرف الباسورد الأصلي. 

الـ hash ده **one-way encryption** → يعني:
- ممكن نعمل hash للباسورد ✅
- مش ممكن نرجع الباسورد الأصلي من الـ hash ❌

**النتيجة:**

بعد ما الـ `create()` تخلص:

- المستخدم الجديد هيبقى موجود في جدول `users` في قاعدة البيانات
- المتغير `$user` هيكون object من نوع `User`
- الـ object ده فيه بيانات المستخدم الجديد

#### مثال 3: تحديث مستخدم (من ProfileController)

<div dir="ltr">

```php
// app/Http/Controllers/ProfileController.php
public function update(ProfileUpdateRequest $request): RedirectResponse
{
    $request->user()->fill($request->validated());

    if ($request->user()->isDirty('email')) {
        $request->user()->email_verified_at = null;
    }

    $request->user()->save();

    return Redirect::route('profile.edit')->with('status', 'profile-updated');
}
```

</div>

---

## 2. View (الفيو) 🎨

### إيه هو الـ View؟

الـ **View** هو الملف اللي بيحتوي على الـ HTML والـ UI اللي المستخدم بيشوفه في المتصفح. في Laravel، الـ Views بتكون بامتداد `.blade.php`.

### العلاقة بين Controller والـ View

**الـ Controller بيمرر البيانات للـ View، والـ View بيعرضها للمستخدم.**

#### مثال من مشروعنا: Profile View

خلينا نشوف الـ View بتاع صفحة البروفايل:

<div dir="ltr">

```php
// resources/views/profile/edit.blade.php
<x-app-layout>
    <x-slot name="header">
        <h2>{{ __('Profile') }}</h2>
    </x-slot>
    
    <div class="py-12">
        <div class="max-w-7xl mx-auto sm:px-6 lg:px-8 space-y-6">
            @include('profile.partials.update-profile-information-form')
        </div>
    </div>
</x-app-layout>
```

</div>

**شرح الكود:**
- الـ View ده بيعرض صفحة تعديل البروفايل
- `$user` - دي المتغير اللي الـ Controller بيمرره للـ View
- `{{ $user->name }}` - بنستخدم الـ Blade syntax عشان نعرض البيانات

#### إزاي الـ Controller بيمرر البيانات للـ View؟

خلينا نشوف الـ Controller الكامل:

<div dir="ltr">

```php
// app/Http/Controllers/ProfileController.php
namespace App\Http\Controllers;

use Illuminate\Http\Request;
use Illuminate\View\View;

class ProfileController extends Controller
{
    public function edit(Request $request): View
    {
        return view('profile.edit', [
            'user' => $request->user(),
        ]);
    }
}
```

</div>

**العلاقة:**
- الـ Controller بيدور على البيانات (المستخدم الحالي)
- بيمررها للـ View في الـ Array الثاني
- الـ View بيعرض البيانات باستخدام Blade syntax

#### مثال على استخدام البيانات في الـ View

<div dir="ltr">

```php
// resources/views/profile/partials/update-profile-information-form.blade.php
<form method="post" action="{{ route('profile.update') }}">
    <div>
        <x-input-label for="name" :value="__('Name')" />
        <x-text-input 
            id="name" 
            name="name" 
            :value="old('name', $user->name)"  // هنا بنعرض اسم المستخدم
            required 
        />
    </div>
    
    <div>
        <x-input-label for="email" :value="__('Email')" />
        <x-text-input 
            id="email" 
            name="email" 
            :value="old('email', $user->email)"  // هنا بنعرض الإيميل
            required 
        />
    </div>
</form>
```

</div>

**شرح:**
- `$user->name` - بنجيب اسم المستخدم من الـ Model
- `$user->email` - بنجيب الإيميل من الـ Model
- `old('name', $user->name)` - لو في validation error، بنرجع القيمة القديمة

---

## 3. Controller (الكونترولر) 🎮

### إيه هو الـ Controller؟

الـ **Controller** هو الكلاس اللي بيستقبل الـ Request من المستخدم، وبعدين:
1. بيدور على البيانات من الـ Model
2. بيعمل المعالجة المطلوبة
3. بيمرر البيانات للـ View أو بيرجع Response

### العلاقة بين Controller والـ View والـ Model

**الـ Controller هو الـ Middleman بين الـ View والـ Model.**

#### مثال شامل: ProfileController

خلينا نشوف مثال كامل من مشروعنا:

<div dir="ltr">

```php
// app/Http/Controllers/ProfileController.php
namespace App\Http\Controllers;

use App\Http\Requests\ProfileUpdateRequest;
use Illuminate\Http\RedirectResponse;
use Illuminate\Http\Request;
use Illuminate\Support\Facades\Auth;
use Illuminate\Support\Facades\Redirect;
use Illuminate\View\View;

class ProfileController extends Controller
{
    /**
     * Display the user's profile form.
     */
    public function edit(Request $request): View
    {
        return view('profile.edit', [
            'user' => $request->user(),
        ]);
    }

    /**
     * Update the user's profile information.
     */
    public function update(ProfileUpdateRequest $request): RedirectResponse
    {
        $request->user()->fill($request->validated());

        if ($request->user()->isDirty('email')) {
            $request->user()->email_verified_at = null;
        }

        $request->user()->save();

        return Redirect::route('profile.edit')->with('status', 'profile-updated');
    }

    /**
     * Delete the user's account.
     */
    public function destroy(Request $request): RedirectResponse
    {
        $request->validateWithBag('userDeletion', [
            'password' => ['required', 'current_password'],
        ]);

        $user = $request->user();

        Auth::logout();

        $user->delete();

        $request->session()->invalidate();

        $request->session()->regenerateToken();

        return Redirect::to('/');
    }
}
```

</div>

### شرح تفصيلي للعملية:

#### 1. عرض الصفحة (GET Request)

<div dir="ltr">

```
المستخدم → Route → Controller.edit() → User Model → Controller → View → HTML
```

</div>

**الخطوات:**
1. المستخدم يضغط على رابط `/profile`
2. الـ Route بيوجهه لـ `ProfileController::edit`
3. الـ Controller بيدور على المستخدم الحالي (`$request->user()`)
4. الـ Model بيدور في قاعدة البيانات
5. الـ Controller بيمرر البيانات للـ View
6. الـ View بيعرض الـ HTML
7. المستخدم بيشوف الصفحة

#### 2. تحديث البيانات (PATCH Request)

<div dir="ltr">

```
المستخدم يملأ الفورم → Submit → Route → Controller.update() → 
User Model → Save في قاعدة البيانات → Redirect → View مع رسالة نجاح
```

</div>

**الخطوات:**
1. المستخدم يعدل البيانات في الفورم
2. يضغط Save
3. الـ Route بيوجهه لـ `ProfileController::update`
4. الـ Controller بيعمل Validation
5. الـ Controller بيحدث الـ Model
6. الـ Model بيعمل `save()` في قاعدة البيانات
7. الـ Controller بيرجع Redirect للـ View
8. الـ View بيعرض رسالة النجاح

---

## 4. Routes (الروابط) 🛣️

### إيه هي الـ Routes؟

الـ **Routes** هي اللي بتحدد أي URL بيوصل لأي Controller method.

#### مثال من مشروعنا:

<div dir="ltr">

```php
// routes/web.php
use App\Http\Controllers\ProfileController;
use Illuminate\Support\Facades\Route;

Route::middleware('auth')->group(function () {
    Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
    Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
    Route::delete('/profile', [ProfileController::class, 'destroy'])->name('profile.destroy');
});
```

</div>

**شرح:**
- `Route::get()` - للصفحات (عرض البيانات)
- `Route::patch()` - لتحديث البيانات
- `Route::delete()` - لحذف البيانات
- `->name()` - اسم الـ Route (بنستخدمه في الـ Views)

---

## 5. دورة حياة الطلب الكاملة 🔄

خلينا نشوف إزاي كل حاجة بتشتغل مع بعض من الأول للآخر:

### مثال: المستخدم عايز يشوف بروفايله

<div dir="ltr">

```
1. المستخدم يكتب: http://localhost/profile
                    ↓
2. Route: GET /profile → ProfileController::edit
                    ↓
3. Controller.edit():
   - بنجيب المستخدم: $user = $request->user()
   - الـ Request بيدور على User Model
                    ↓
4. User Model:
   - بيدور في جدول users في قاعدة البيانات
   - بيرجع البيانات (id, name, email, ...)
                    ↓
5. Controller:
   - بيستلم البيانات من الـ Model
   - بيمررها للـ View: return view('profile.edit', ['user' => $user])
                    ↓
6. View (profile/edit.blade.php):
   - بيستلم البيانات من الـ Controller
   - بيعرضها في HTML: {{ $user->name }}, {{ $user->email }}
                    ↓
7. المستخدم بيشوف الصفحة في المتصفح
```

</div>

### مثال: المستخدم عايز يحدث بروفايله

<div dir="ltr">

```
1. المستخدم يملأ الفورم ويضغط Save
                    ↓
2. Form Submit: method="POST" action="{{ route('profile.update') }}"
                    ↓
3. Route: PATCH /profile → ProfileController::update
                    ↓
4. Controller.update():
   - بنعمل Validation على البيانات
   - بنجيب المستخدم: $user = $request->user()
   - بنحدث البيانات: $user->fill($request->validated())
                    ↓
5. User Model:
   - بنحفظ في قاعدة البيانات: $user->save()
   - الـ Model بيعمل UPDATE query على جدول users
                    ↓
6. Controller:
   - بنرجع Redirect: Redirect::route('profile.edit')->with('status', 'profile-updated')
                    ↓
7. Route: GET /profile → ProfileController::edit (مرة تانية)
                    ↓
8. View:
   - بيعرض الصفحة مع رسالة النجاح
   - @if (session('status') === 'profile-updated') → "Saved."
                    ↓
9. المستخدم بيشوف الصفحة محدثة مع رسالة النجاح
```

</div>

---

## 6. أمثلة عملية من المشروع 💡

### مثال 1: Dashboard

<div dir="ltr">

```php
// routes/web.php
Route::get('/dashboard', function () {
    return view('dashboard');  // View بسيط بدون بيانات
})->middleware(['auth', 'verified'])->name('dashboard');
```

</div>

**الملفات المشاركة:**
- **Route:** `routes/web.php` - يحدد الـ URL
- **View:** `resources/views/dashboard.blade.php` - يعرض الصفحة

### مثال 2: Profile (مثال كامل من المشروع)

**الـ Route:**

<div dir="ltr">

```php
// routes/web.php
Route::get('/profile', [ProfileController::class, 'edit'])->name('profile.edit');
Route::patch('/profile', [ProfileController::class, 'update'])->name('profile.update');
```

</div>

**الـ Controller:**

<div dir="ltr">

```php
// app/Http/Controllers/ProfileController.php
public function edit(Request $request): View
{
    return view('profile.edit', [
        'user' => $request->user(),
    ]);
}

public function update(ProfileUpdateRequest $request): RedirectResponse
{
    $request->user()->fill($request->validated());
    $request->user()->save();
    return Redirect::route('profile.edit')->with('status', 'profile-updated');
}
```

</div>

**الـ View (استخدام البيانات):**

<div dir="ltr">

```php
// resources/views/profile/partials/update-profile-information-form.blade.php
<x-text-input :value="old('name', $user->name)" />
```

</div>

---

## 7. ملخص العلاقات 🔗

### Model ↔ Database Table

<div dir="ltr">

```
User Model (app/Models/User.php)
        ↕
users Table (database/migrations/...)
```

</div>

- كل Model مربوط بجدول في قاعدة البيانات
- كل خاصية (property) في الـ Model = عمود (column) في الجدول

### Controller ↔ View

<div dir="ltr">

```
Controller (ProfileController::edit())
        ↓ (يمرر البيانات)
View (profile/edit.blade.php)
        ↓ (يعرض HTML)
المستخدم
```

</div>

- الـ Controller بيمرر البيانات للـ View
- الـ View بيعرض البيانات للمستخدم

### Controller ↔ Model

<div dir="ltr">

```
Controller (ProfileController::update())
        ↓ (يطلب البيانات)
Model (User)
        ↓ (يدور في قاعدة البيانات)
Database Table (users)
```

</div>

- الـ Controller بيدور على البيانات من الـ Model
- الـ Model بيدور في قاعدة البيانات

### Route → Controller → Model → View

<div dir="ltr">

```
Route (GET /profile)
    ↓
Controller.edit()
    ↓
Model (User) → Database
    ↓
View (profile/edit.blade.php)
    ↓
HTML → المستخدم
```

</div>

---

## 8. فوائد استخدام MVC ✨

1. **التنظيم:** كل جزء في مكانه المناسب
2. **إعادة الاستخدام:** ممكن نستخدم نفس الـ Model في أماكن كتيرة
3. **سهولة الصيانة:** لو محتاج تعدل في حاجة، هتعرف تدور عليها فوراً
4. **الفصل بين الاهتمامات:** الـ UI منفصل عن المنطق (Logic)
5. **سهولة الاختبار:** كل جزء ممكن نختبره لوحده

---

## 9. نصائح مهمة 💡

### ⚠️ لا تفعل هذا:

**لا تضع المنطق (Logic) في الـ View** - لازم يكون في الـ Controller.

### ✅ افعل هذا (مثال من مشروعنا):

**في الـ Controller:** نجيب البيانات ونمررها للـ View

<div dir="ltr">

```php
// app/Http/Controllers/ProfileController.php
public function edit(Request $request): View
{
    return view('profile.edit', [
        'user' => $request->user(),  // المنطق في Controller
    ]);
}
```

</div>

**في الـ View:** فقط العرض

<div dir="ltr">

```php
// resources/views/profile/partials/update-profile-information-form.blade.php
<x-text-input :value="old('name', $user->name)" />
```

</div>

### قاعدة ذهبية:

- **Model:** البيانات والقواعد (Rules)
- **View:** العرض فقط (Display)
- **Controller:** الربط بينهم (Connection)

---

## 10. أسئلة شائعة ❓

### س: ليه محتاجين MVC أصلاً؟

**ج:** عشان ننظم الكود ونسهل الصيانة. لو كل حاجة في ملف واحد، هيبقى صعب جداً نعدل فيه بعدين.

### س: كل Model لازم يكون مربوط بجدول؟

**ج:** معظم الأحيان أيوة، بس ممكن يكون عندك Model مش مربوط بجدول (مثل Validation Models).

### س: ممكن أستخدم Model بدون Controller؟

**ج:** ممكن، بس ده مش أفضل practice. الأفضل الـ Controller يربط كل حاجة.

### س: إيه الفرق بين Route و Controller؟

**ج:** الـ Route بيحدد الـ URL والـ HTTP Method، والـ Controller بيحتوي على المنطق (Logic).

---

## خاتمة 🎉

ماشي، كده فهمنا MVC! خلينا نلخص:

1. **Model** = البيانات في قاعدة البيانات
2. **View** = الـ HTML اللي المستخدم بيشوفه
3. **Controller** = اللي بيربط بينهم
4. **Route** = يحدد أي URL يوصل لأي Controller

**اتذكر:** الـ Controller هو الـ Middleman بين الـ View والـ Model!

---

## مثال سريع للتذكر 🚀

<div dir="ltr">

```
المستخدم يضغط على رابط
    ↓
Route (يحدد الطريق)
    ↓
Controller (يدور على البيانات)
    ↓
Model (يدور في قاعدة البيانات)
    ↓
Controller (يمرر البيانات)
    ↓
View (يعرض البيانات)
    ↓
المستخدم بيشوف النتيجة
```

</div>

**هتقول إيه لو فهمت؟** 😊

---

*تم إعداد هذا الشرح بناءً على مشروع Laravel الخاص بك*

</div>
