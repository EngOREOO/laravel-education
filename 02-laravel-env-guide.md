<div dir="rtl">

# 📝 شرح ملف .env في لارافيل

### دليل شامل للمتغيرات والإعدادات

الملف ده هيشرحلك كل حاجة عن ملف `.env` في لارافيل، المتغيرات اللي فيه، وإزاي تستخدمها صح.

---

## 🤔 إيه هو ملف .env؟

ملف `.env` ده ملف بيحتوي على كل الإعدادات الحساسة والمهمة في مشروعك زي:
- بيانات الاتصال بالداتابيز
- مفاتيح الأمان (Security Keys)
- إعدادات البريد الإلكتروني
- إعدادات البيئة (Development / Production)

**ليه مهم؟**
- عشان متحطش معلومات حساسة زي الباسوردات في الكود مباشرة
- عشان تقدر تغير الإعدادات بسهولة من غير ما تمس الكود
- عشان كل مطور يقدر يستخدم إعدادات خاصة بيه

---

## ⚙️ المتغيرات الأساسية

### 1️⃣ إعدادات التطبيق (Application Settings)

#### APP_NAME
```env
APP_NAME=Laravel
```
**الشرح:** اسم التطبيق بتاعك، بيظهر في صفحات الويب والإيميلات

**مثال:**
```env
APP_NAME="متجر الإلكترونيات"
```

---

#### APP_ENV
```env
APP_ENV=local
```
**الشرح:** بيحدد بيئة التشغيل

**القيم المتاحة:**
- `local` - للتطوير على جهازك الشخصي
- `production` - للسيرفر الحقيقي (Live)
- `testing` - للتست
- `staging` - بيئة تجريبية قبل النشر

**مثال:**
```env
APP_ENV=production
```

---

#### APP_KEY
```env
APP_KEY=base64:xxxxxxxxxxxxxxxxxxxxx
```
**الشرح:** مفتاح التشفير الأساسي للتطبيق (مهم جداً للأمان!)

**ملاحظات مهمة:**
- لو الملف جديد ومفيهوش APP_KEY، اكتب الأمر ده:
  ```bash
  php artisan key:generate
  ```
- لازم يكون سري تماماً
- لو غيرته، كل البيانات المشفرة القديمة مش هتشتغل

---

#### APP_DEBUG
```env
APP_DEBUG=true
```
**الشرح:** بيظهر تفاصيل الأخطاء على الشاشة

**القيم:**
- `true` - يظهر تفاصيل الأخطاء (للتطوير فقط!)
- `false` - يخفي التفاصيل (للسيرفر الحقيقي)

**تحذير:** لازم يكون `false` على السيرفر الحقيقي عشان الأمان!

---

#### APP_URL
```env
APP_URL=http://localhost
```
**الشرح:** رابط الموقع الأساسي

**أمثلة:**
```env
APP_URL=http://localhost:8000
APP_URL=https://mywebsite.com
```

---

### 2️⃣ إعدادات قاعدة البيانات (Database Settings)

#### DB_CONNECTION
```env
DB_CONNECTION=mysql
```
**الشرح:** نوع الداتابيز اللي بتستخدمه

**القيم المتاحة:**
- `mysql` - ماي إس كيو إل (الأشهر)


---

#### DB_HOST
```env
DB_HOST=127.0.0.1
```
**الشرح:** عنوان السيرفر اللي عليه الداتابيز

**القيم الشائعة:**
- `127.0.0.1` أو `localhost` - للداتابيز المحلية
- `192.168.x.x` - لداتابيز على الشبكة المحلية
- `db.example.com` - لداتابيز خارجية

---

#### DB_PORT
```env
DB_PORT=3306
```
**الشرح:** رقم المنفذ (Port) للاتصال

**البورتات الافتراضية:**
- `3306` - MySQL
- `5432` - PostgreSQL
- `1433` - SQL Server

---

#### DB_DATABASE
```env
DB_DATABASE=laravel
```
**الشرح:** اسم الداتابيز اللي هتستخدمها

**مثال:**
```env
DB_DATABASE=my_ecommerce_db
```

---

#### DB_USERNAME
```env
DB_USERNAME=root
```
**الشرح:** اسم المستخدم للدخول على الداتابيز

**أمثلة:**
```env
DB_USERNAME=root
DB_USERNAME=admin
DB_USERNAME=laravel_user
```

---

#### DB_PASSWORD
```env
DB_PASSWORD=
```
**الشرح:** كلمة المرور للداتابيز

**أمثلة:**
```env
DB_PASSWORD=
DB_PASSWORD=my_secure_password123
```

**ملحوظة:** لو فاضي يعني مفيش باسورد (زي XAMPP الافتراضي)

---

### 3️⃣ إعدادات الكاش (Cache Settings)

#### CACHE_DRIVER
```env
CACHE_DRIVER=file
```
**الشرح:** طريقة حفظ الكاش (البيانات المؤقتة)

**القيم المتاحة:**
- `file` - يحفظ في ملفات (بسيط للتطوير)
- `redis` - يستخدم Redis (سريع جداً)
- `memcached` - يستخدم Memcached
- `database` - يحفظ في الداتابيز
- `array` - مؤقت في الذاكرة (للتست فقط)

---

### 4️⃣ إعدادات الجلسة (Session Settings)

#### SESSION_DRIVER
```env
SESSION_DRIVER=file
```
**الشرح:** طريقة حفظ بيانات الجلسات (Sessions)

**القيم المتاحة:**
- `file` - يحفظ في ملفات
- `cookie` - يحفظ في الكوكيز
- `database` - يحفظ في الداتابيز
- `redis` - يستخدم Redis

---

#### SESSION_LIFETIME
```env
SESSION_LIFETIME=120
```
**الشرح:** مدة بقاء الجلسة بالدقايق

**أمثلة:**
```env
SESSION_LIFETIME=120    # ساعتين
SESSION_LIFETIME=1440   # يوم كامل
SESSION_LIFETIME=10080  # أسبوع
```

---

### 5️⃣ إعدادات الطابور (Queue Settings)

#### QUEUE_CONNECTION
```env
QUEUE_CONNECTION=sync
```
**الشرح:** طريقة معالجة المهام في الخلفية

**القيم المتاحة:**
- `sync` - ينفذ المهام فوراً (للتطوير)
- `database` - يحفظ المهام في الداتابيز
- `redis` - يستخدم Redis (الأسرع)
- `sqs` - Amazon SQS

---

### 6️⃣ إعدادات البريد الإلكتروني (Mail Settings)

#### MAIL_MAILER
```env
MAIL_MAILER=smtp
```
**الشرح:** طريقة إرسال الإيميلات

**القيم:**
- `smtp` - بروتوكول SMTP العادي
- `sendmail` - Sendmail
- `mailgun` - خدمة Mailgun
- `ses` - Amazon SES
- `log` - يحفظ الإيميل في ملف Log (للتست)

---

#### MAIL_HOST
```env
MAIL_HOST=smtp.gmail.com
```
**الشرح:** عنوان سيرفر البريد

**أمثلة شائعة:**
```env
MAIL_HOST=smtp.gmail.com        # Gmail
MAIL_HOST=smtp.office365.com    # Outlook
MAIL_HOST=smtp.mailtrap.io      # Mailtrap (للتست)
```

---

#### MAIL_PORT
```env
MAIL_PORT=587
```
**الشرح:** رقم المنفذ لسيرفر البريد

**البورتات الشائعة:**
- `587` - TLS (الأكثر استخداماً)
- `465` - SSL
- `25` - بدون تشفير (غير آمن)

---

#### MAIL_USERNAME & MAIL_PASSWORD
```env
MAIL_USERNAME=your_email@gmail.com
MAIL_PASSWORD=your_app_password
```
**الشرح:** بيانات تسجيل الدخول لحساب البريد

**ملحوظة مهمة لـ Gmail:**
- لازم تستخدم App Password مش الباسورد العادي
- اتبع الخطوات من إعدادات الأمان في جوجل

---

#### MAIL_ENCRYPTION
```env
MAIL_ENCRYPTION=tls
```
**الشرح:** نوع التشفير

**القيم:**
- `tls` - Transport Layer Security
- `ssl` - Secure Sockets Layer
- `null` - بدون تشفير

---

#### MAIL_FROM_ADDRESS & MAIL_FROM_NAME
```env
MAIL_FROM_ADDRESS=noreply@example.com
MAIL_FROM_NAME="${APP_NAME}"
```
**الشرح:** الإيميل والاسم اللي هيظهر كمُرسل

**مثال:**
```env
MAIL_FROM_ADDRESS=support@mystore.com
MAIL_FROM_NAME="متجري الإلكتروني"
```

---

### 7️⃣ إعدادات خارجية (Third-party Services)

#### AWS (Amazon Web Services)
```env
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
```
**الشرح:** إعدادات AWS لتخزين الملفات أو الخدمات السحابية

---

#### Pusher (WebSockets)
```env
PUSHER_APP_ID=
PUSHER_APP_KEY=
PUSHER_APP_SECRET=
PUSHER_APP_CLUSTER=mt1
```
**الشرح:** إعدادات Pusher للإشعارات الفورية والـ Real-time

---

## 🔒 نصائح الأمان

### ✅ اللي لازم تعمله:

1. **متشاركش ملف .env أبداً**
   - لازم يكون في `.gitignore`
   - متحطوش على GitHub أو أي مكان عام

2. **استخدم باسوردات قوية**
   ```env
   DB_PASSWORD=X9$mK2#pL8@qR5
   ```

3. **غيّر APP_DEBUG على السيرفر الحقيقي**
   ```env
   APP_DEBUG=false
   ```

4. **استخدم HTTPS على السيرفر الحقيقي**
   ```env
   APP_URL=https://mywebsite.com
   ```

### ❌ اللي متعملوش:

1. متحطش معلومات حساسة في الكود
2. متستخدمش نفس الإعدادات في التطوير والإنتاج
3. متسيبش APP_DEBUG شغال على السيرفر الحقيقي
4. متشاركش الـ APP_KEY مع حد

---

## 🎯 أمثلة عملية

### مثال 1: إعدادات التطوير المحلي
```env
APP_NAME="My Laravel App"
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=my_app_db
DB_USERNAME=root
DB_PASSWORD=

MAIL_MAILER=log
```

---

### مثال 2: إعدادات السيرفر الحقيقي
```env
APP_NAME="My Laravel App"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://mywebsite.com

DB_CONNECTION=mysql
DB_HOST=production-db-server.com
DB_PORT=3306
DB_DATABASE=prod_database
DB_USERNAME=prod_user
DB_PASSWORD=super_secure_password_here

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=noreply@mywebsite.com
MAIL_PASSWORD=app_specific_password

CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=redis
```

---

## 📚 كيفية استخدام المتغيرات في الكود

### في ملفات الـ Config
```php
// config/database.php
'mysql' => [
    'host' => env('DB_HOST', '127.0.0.1'),
    'database' => env('DB_DATABASE', 'forge'),
],
```

### في الكود مباشرة
```php
// الطريقة الصحيحة
$appName = config('app.name');
$dbHost = config('database.connections.mysql.host');

// أو
$appName = env('APP_NAME', 'Laravel');
```

**ملحوظة:** الأفضل تستخدم `config()` مش `env()` في الكود

---

## 🔄 نسخ الملف لمطور جديد

لما يجي مطور جديد على المشروع:

1. انسخ ملف `.env.example` لـ `.env`:
   ```bash
   cp .env.example .env
   ```

2. اعمل Generate للـ APP_KEY:
   ```bash
   php artisan key:generate
   ```

3. ظبط إعدادات الداتابيز الخاصة بيه

4. شغّل الـ migrations:
   ```bash
   php artisan migrate
   ```

---

## 🐛 حل المشاكل الشائعة

### المشكلة: التغييرات مش بتظهر
**الحل:** امسح الكاش
```bash
php artisan config:clear
php artisan cache:clear
```

### المشكلة: "Please provide a valid app key"
**الحل:** اعمل generate للـ key
```bash
php artisan key:generate
```

### المشكلة: مش عارف أتصل بالداتابيز
**الحل:** تأكد من:
- MySQL شغال
- اسم الداتابيز صح
- اليوزر والباسورد صح
- البورت صح (3306)

---

## 📖 مصادر إضافية

- [Laravel Documentation - Configuration](https://laravel.com/docs/configuration)
- [Laravel Documentation - Environment](https://laravel.com/docs/configuration#environment-configuration)

---

**صُنع بحب ❤️ لمساعدة مطوري لارافيل**

</div>
